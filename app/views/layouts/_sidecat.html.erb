<div class="sidenav">
    <div class="links">
    <h3><%= link_to "Add New Product", new_product_path %></h3>
    <h3><u>Categories</u></h3>
        <% Product::CATEGORIES.each do |cat| %>
      <p><%= link_to cat, :search => cat %></p>
      <% end %>
      
    




    </div>
</div>